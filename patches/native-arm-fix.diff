=== modified file 'rootstock'
--- rootstock	2010-05-20 00:35:07 +0000
+++ rootstock	2010-05-20 00:52:46 +0000
@@ -825,7 +825,9 @@
 
 echo "${FQDN}" >$MOUNTPOINT/etc/hostname
 
-touch $MOUNTPOINT/etc/is_native
+if [ "$IS_NATIVE" ];then
+    touch $MOUNTPOINT/etc/is_native
+fi
 
 if [ "$KERNEL_IMAGE" ];then
     setup_kernel_image

