=== modified file 'rootstock'
--- rootstock	2011-07-06 15:55:30 +0000
+++ rootstock	2011-07-06 15:58:35 +0000
@@ -1198,6 +1198,8 @@
 root
 ROOTPASSWD
 
+echo "S:2345:respawn:/sbin/getty 115200 ${SERIAL_TTYS}" >> /etc/inittab
+
 #if which dpkg-vendor >/dev/null 2>&1 && [ "`dpkg-vendor --query vendor`" = Ubuntu ]; then
 else
   passwd -l root || true

